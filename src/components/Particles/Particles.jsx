import './config.json';
import './particles';

const Particles = () => {
    return (
        <>
            <div id="particles-js" className="particles-js"></div>
        </>
    );
};

export default Particles;